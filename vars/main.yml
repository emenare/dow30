mysql_replication_master_host: "{{  hosts['mysqlmaster'] }}"
mysql_replication_user:
  - name: root
    host: "%"
    password: root
apache_listen_port: 80
apache_vhosts:
  - {servername: "dow30", documentroot: "/var/www/dow30"}
